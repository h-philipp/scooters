app:
  build:
    image: registry.heroku.com/scooter-app/web
    dockerfile: Dockerfile
  encrypted_env_file:
    - deployment.env.encrypted

heroku-dockercfg-generator:
  image: codeship/heroku-dockercfg-generator
  add_docker: true
  encrypted_env_file:
    - deployment.env.encrypted